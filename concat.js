const first = 'Johirul Islam';
const last = 'Joy';

// const fullname = first + last;
const fullname = first + ' ' + last;
console.log(fullname);

const fullname2 = first.concat(' ').concat(last);
console.log(fullname2);

// includes example

console.log(last.includes('x'));